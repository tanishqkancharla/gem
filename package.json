{
  "name": "editor",
  "version": "1.0.0",
  "description": "A plain text editor, built with Prosemirror",
  "main": "index.js",
  "scripts": {
    "dev": "mkdir -p dist && cp public/* dist && esbuild src/index.ts --bundle --sourcemap --outfile=dist/index.js && npx serve -l 3000 dist",
    "build": "mkdir -p dist && cp public/* dist && esbuild src/index.ts --bundle --minify --outfile=dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "prosemirror-commands": "^1.1.12",
    "prosemirror-history": "^1.2.0",
    "prosemirror-inputrules": "^1.1.3",
    "prosemirror-keymap": "^1.1.5",
    "prosemirror-model": "^1.15.0",
    "prosemirror-state": "^1.3.4",
    "prosemirror-view": "^1.23.2"
  },
  "devDependencies": {
    "esbuild": "^0.13.15",
    "@types/prosemirror-commands": "^1.0.4",
    "@types/prosemirror-history": "^1.0.3",
    "@types/prosemirror-inputrules": "^1.0.4",
    "@types/prosemirror-keymap": "^1.0.4",
    "@types/prosemirror-model": "^1.13.2",
    "@types/prosemirror-state": "^1.2.8",
    "@types/prosemirror-view": "^1.19.1"
  }
}
