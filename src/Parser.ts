// class TKParser extends Parser {

// 	createParse(
// 		input: Input,
// 		fragments: readonly TreeFragment[],
// 		ranges: readonly { from: number; to: number }[]
// 	): PartialParse {
// 		const doc = input.read(0, input.length);
// 	}

// }
