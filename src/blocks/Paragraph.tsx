import styled from "styled-components";
import { articleItemMargin } from "../styles/vars";

export const P = styled.p`
	margin-top: ${articleItemMargin};
	margin-bottom: ${articleItemMargin};
	line-height: 1.4rem;
	font-size: 1.025rem;
`;
